  /* data.window.essentialData.APIS.API_BUILDER.defaults.headers[":path:"] = "0" */
  /* data.window.essentialData.APIS.API_BUILDER.defaults.timeout = 1 */
  /* data.window.essentialData.APIS.API.defaults.transformRequest = (data, headers) => {
    headers.Authorization = "Teste"
    console.log({ data, headers })
  } */
  /* const html = win.document.querySelector("html")
  if (html) {
    html.remove?.()
  }
  return "Foi" */
  /* eiyJhbGciOiJIUzI1NiIsInF5cCI9IkpXVCJ9EF2022 */

  /* 
  (() => {
    window.essentialData.APIS.API.interceptors.request.use(
    async (config) => {
        config.headers.Authorization = "eiyJhbGciOiJIUzI1NiIsInF5cCI9IkpXVCJ9EF2022"
        if(config.url.includes("/promotions/list-front-site")){
            config.timeout = 1
            return config
        }
        return config
      },
    (error) => {
        return Promise.reject(error)
      }
    )
})()
  */